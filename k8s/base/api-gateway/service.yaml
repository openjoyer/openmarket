apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: openmarket
spec:
  # единственная внешняя точка входа в кластер (аналог host-порта 8081 в Compose)
  type: NodePort
  selector:
    app: api-gateway
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
